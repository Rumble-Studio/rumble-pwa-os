import { FocusoutDirective } from './focusout.directive';

describe('FocusoutDirective', () => {
	it('should create an instance', () => {
		const directive = new FocusoutDirective();
		expect(directive).toBeTruthy();
	});
});
